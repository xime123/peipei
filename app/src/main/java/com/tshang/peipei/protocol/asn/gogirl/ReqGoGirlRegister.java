//-----------------------------------------------------------------------------
//   NOTE: this is a machine generated file - editing not recommended
//
//   File: ./src/com/tshang/peipei/protocol/asn/gogirl/ReqGoGirlRegister.java
//
//   Java class for ASN.1 definition ReqGoGirlRegister as defined in
//   module GOGIRL.
//   This file was generated by Snacc for Java at Tue Jan 26 18:38:16 2016
//-----------------------------------------------------------------------------

package com.tshang.peipei.protocol.asn.gogirl;

// Import PrintStream class for print methods
import java.io.PrintStream;

// Import ASN.1 basic type representations
import com.ibm.util.*;

// Import ASN.1 decoding/encoding classes
import com.ibm.asn1.*;

/** This class represents the ASN.1 SEQUENCE type <tt>ReqGoGirlRegister</tt>.
  * For each sequence member, sequence classes contain a
  * public member variable of the corresponding Java type.
  * @author Snacc for Java
  * @version Tue Jan 26 18:38:16 2016

  */

public class ReqGoGirlRegister implements ASN1Type {

  /** member variable representing the sequence member username of type byte[] */
  public byte[] username;
  /** member variable representing the sequence member passwd of type byte[] */
  public byte[] passwd;
  /** member variable representing the sequence member msgcode of type byte[] */
  public byte[] msgcode;
  /** member variable representing the sequence member nickname of type byte[] */
  public byte[] nickname;
  /** member variable representing the sequence member checksum of type byte[] */
  public byte[] checksum;
  /** member variable representing the sequence member sex of type java.math.BigInteger */
  public java.math.BigInteger sex;
  /** member variable representing the sequence member from of type java.math.BigInteger */
  public java.math.BigInteger from;
  /** member variable representing the sequence member phonebrand of type byte[] */
  public byte[] phonebrand;
  /** member variable representing the sequence member osver of type byte[] */
  public byte[] osver;
  /** member variable representing the sequence member imei of type byte[] */
  public byte[] imei;
  /** member variable representing the sequence member appver of type java.math.BigInteger */
  public java.math.BigInteger appver;
  /** member variable representing the sequence member inviteuid of type java.math.BigInteger */
  public java.math.BigInteger inviteuid;

  /** default constructor */
  public ReqGoGirlRegister() {}

  /** copy constructor */
  public ReqGoGirlRegister (ReqGoGirlRegister arg) {
    username = new byte[arg.username.length];
    System.arraycopy(arg.username,0,username,0,arg.username.length);
    passwd = new byte[arg.passwd.length];
    System.arraycopy(arg.passwd,0,passwd,0,arg.passwd.length);
    msgcode = new byte[arg.msgcode.length];
    System.arraycopy(arg.msgcode,0,msgcode,0,arg.msgcode.length);
    nickname = new byte[arg.nickname.length];
    System.arraycopy(arg.nickname,0,nickname,0,arg.nickname.length);
    checksum = new byte[arg.checksum.length];
    System.arraycopy(arg.checksum,0,checksum,0,arg.checksum.length);
    sex = arg.sex;
    from = arg.from;
    phonebrand = new byte[arg.phonebrand.length];
    System.arraycopy(arg.phonebrand,0,phonebrand,0,arg.phonebrand.length);
    osver = new byte[arg.osver.length];
    System.arraycopy(arg.osver,0,osver,0,arg.osver.length);
    imei = new byte[arg.imei.length];
    System.arraycopy(arg.imei,0,imei,0,arg.imei.length);
    appver = arg.appver;
    inviteuid = arg.inviteuid;
  }

  /** encoding method.
    * @param enc
    *        encoder object derived from com.ibm.asn1.ASN1Encoder
    * @exception com.ibm.asn1.ASN1Exception 
    *            encoding error
    */
  public void encode (ASN1Encoder enc) throws ASN1Exception {
    int seq_nr = enc.encodeSequence();
    enc.encodeOctetString(username);
    enc.encodeOctetString(passwd);
    enc.encodeOctetString(msgcode);
    enc.encodeOctetString(nickname);
    enc.encodeOctetString(checksum);
    enc.encodeInteger(sex);
    enc.encodeInteger(from);
    enc.encodeOctetString(phonebrand);
    enc.encodeOctetString(osver);
    enc.encodeOctetString(imei);
    enc.encodeInteger(appver);
    enc.encodeInteger(inviteuid);
    enc.endOf(seq_nr);
  }

  /** decoding method.
    * @param dec
    *        decoder object derived from com.ibm.asn1.ASN1Decoder
    * @exception com.ibm.asn1.ASN1Exception 
    *            decoding error
    */
  public void decode (ASN1Decoder dec) throws ASN1Exception {
    int seq_nr = dec.decodeSequence();
    username = dec.decodeOctetString();
    passwd = dec.decodeOctetString();
    msgcode = dec.decodeOctetString();
    nickname = dec.decodeOctetString();
    checksum = dec.decodeOctetString();
    sex = dec.decodeInteger();
    from = dec.decodeInteger();
    phonebrand = dec.decodeOctetString();
    osver = dec.decodeOctetString();
    imei = dec.decodeOctetString();
    appver = dec.decodeInteger();
    inviteuid = dec.decodeInteger();
    dec.endOf(seq_nr);
  }

  /** print method (variable indentation)
    * @param os
    *        PrintStream representing the print destination (file, etc)
    * @param indent
    *        number of blanks that preceed each output line.
    */
  public void print (PrintStream os, int indent) {
    os.println("{ -- SEQUENCE --");
    for(int ii = 0; ii < indent+2; ii++) os.print(' ');
    os.print("username = ");
    os.print(Hex.toString(username));
    os.println(',');
    for(int ii = 0; ii < indent+2; ii++) os.print(' ');
    os.print("passwd = ");
    os.print(Hex.toString(passwd));
    os.println(',');
    for(int ii = 0; ii < indent+2; ii++) os.print(' ');
    os.print("msgcode = ");
    os.print(Hex.toString(msgcode));
    os.println(',');
    for(int ii = 0; ii < indent+2; ii++) os.print(' ');
    os.print("nickname = ");
    os.print(Hex.toString(nickname));
    os.println(',');
    for(int ii = 0; ii < indent+2; ii++) os.print(' ');
    os.print("checksum = ");
    os.print(Hex.toString(checksum));
    os.println(',');
    for(int ii = 0; ii < indent+2; ii++) os.print(' ');
    os.print("sex = ");
    os.print(sex.toString());
    os.println(',');
    for(int ii = 0; ii < indent+2; ii++) os.print(' ');
    os.print("from = ");
    os.print(from.toString());
    os.println(',');
    for(int ii = 0; ii < indent+2; ii++) os.print(' ');
    os.print("phonebrand = ");
    os.print(Hex.toString(phonebrand));
    os.println(',');
    for(int ii = 0; ii < indent+2; ii++) os.print(' ');
    os.print("osver = ");
    os.print(Hex.toString(osver));
    os.println(',');
    for(int ii = 0; ii < indent+2; ii++) os.print(' ');
    os.print("imei = ");
    os.print(Hex.toString(imei));
    os.println(',');
    for(int ii = 0; ii < indent+2; ii++) os.print(' ');
    os.print("appver = ");
    os.print(appver.toString());
    os.println(',');
    for(int ii = 0; ii < indent+2; ii++) os.print(' ');
    os.print("inviteuid = ");
    os.print(inviteuid.toString());
    os.println();
    for(int ii = 0; ii < indent; ii++) os.print(' ');
    os.print('}');
  }

  /** default print method (fixed indentation)
    * @param os
    *        PrintStream representing the print destination (file, etc)
    */
  public void print (PrintStream os) {
    print(os,0);
  }

  /** toString method
    * @return the output of {@link #print(PrintStream) print} method (fixed indentation) as a string
    */
  public String toString () {
    java.io.ByteArrayOutputStream baos = new java.io.ByteArrayOutputStream();
    PrintStream ps = new PrintStream(baos);
    print(ps);
    ps.close();
    return baos.toString();
  }

}
